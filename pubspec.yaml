name: docscan
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.8.0+1
  fl_chart: ^1.0.0
  flutter_svg: ^2.2.0
  go_router: ^15.2.0
  google_fonts: ^6.2.1
  http: ^1.4.0
  intl: ^0.20.2
  provider: ^6.1.5
  shared_preferences: ^2.5.3
  syncfusion_flutter_charts: ^29.2.11
  uuid: ^4.5.1
  foundation: ^0.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    # KORRIGIERT: Pfad für Assets
    - assets/images/megusta.png